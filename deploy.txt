# 发版关注路径：这些路径有变更时，auto-release 工作流才会创建新 tag。
# 每行一个目录或文件，空行与 # 注释被忽略。
#
# ⚠️ 本文件同时是 build.yml 里 partial-sync-repo 的 sync_list ——
#    只有列在这里的路径会被同步到更新仓库（AliceJump/ok-ap-update），
#    而更新仓库是 pyappify 安装 / 更新应用时的拉取源。
#    因此「运行时用得到的文件」必须列全，漏掉会直接导致安装失败
#    （v0.2.0 就因缺 main.py / requirements.txt 而构建失败）。
#    ok / pyappify 两个目录由 inline_ok_requirements 在构建期自动追加，不必手写。
#
# 运行时必需：src / assets / i18n / icons / main.py / requirements.txt
docs
src
i18n
assets
icons
main.py
main_debug.py
requirements.txt
README.md
pyappify.yml
pyproject.toml
